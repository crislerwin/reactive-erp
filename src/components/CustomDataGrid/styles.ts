import { tv } from "tailwind-variants";

export const tableClass = tv({
  base: "bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-md shadow-sm",
});

export const headerCellClass = tv({
  base: "bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 font-bold border-b-1 border-gray-200 dark:border-gray-700",
});
