import { describe, expect, test } from "vitest";

describe("Company Routes", () => {
  describe("Get by Id", () => {
    test("Should get users", () => {
      expect(1).toEqual(1);
    });
  });
});
